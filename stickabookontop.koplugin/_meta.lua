local _ = require("gettext")
return {
    fullname = _("Stick a book on top"),
    description = _([[Pin books and folders to the top of the file browser. Long-press a book (up to 4) or a folder (up to 2) to pin it to a chosen position; pinned entries are marked with a pushpin at their top-left corner. Pinned books outside the HOME folder also get a shortcut at the top of HOME, marked with an open-in-new badge at its top-right corner.]]),
}
